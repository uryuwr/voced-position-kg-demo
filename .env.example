# 配置真源已迁到 backend 包内（支持 backend 独立部署）：
#
#   cp backend/.env.example backend/.env
#   # 编辑 backend/.env 后启动 API
#
# 详见 backend/README.md、backend/.env.example
# 仓库根 .env 仅作 monorepo 本地可选兜底，且会被 backend/.env 覆盖。
