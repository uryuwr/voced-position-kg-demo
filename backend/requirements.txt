# backend 独立部署依赖（不含爬虫专用库）
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.0.0
python-dotenv>=1.0.0
psycopg[binary]>=3.1.0
httpx>=0.27.0

# 学员诊断 Agent：AI 网关（OpenAI 兼容）+ LangGraph create_react_agent
langchain-core>=0.3.0
langchain-openai>=0.2.0
langgraph>=0.2.0
openai>=1.40.0

# 可选：历史 Neo 迁移对照（读路径已切 PG）
# neo4j>=5.26.0
