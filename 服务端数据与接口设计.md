# 服务端 · 完整数据结构设计 & 接口设计

> 版本：1.0 · 2026-08-06  
> 范围：前台学员端 + 后台管理端（服务端视角）  
> **用户体系**：外部用户中心已存在；本服务**不自建账号体系**，业务表仅冗余 `user_id` + `user_name`。  
> 认证：网关/用户中心下发 Token；本服务校验后解析出 `user_id`，写操作可选带 `user_name` 快照。

---

## 1. 架构约定

### 1.1 双存储

| 存储 | 内容 | 技术建议 |
| --- | --- | --- |
| **KG（领域知识）** | 行业 / 专业 / 岗位 / 技能 /（后置课·证）及边 | 现有 SQLite SoR + Neo4j 查询；演进可 Postgres JSONB + Neo4j |
| **业务库 Biz** | 诊断、路径、进度、成就、审核提案、运营事件 | **新建** Postgres（推荐）或 MySQL |

```
用户中心 ──Token──▶ API Gateway ──▶ 本服务
                                      ├─ /v1/kg/**        → Neo4j/SQLite
                                      └─ /v1/biz/**       → 业务库
                                              user_id / user_name 仅冗余引用
```

### 1.2 用户字段规范（全表统一）

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `user_id` | string(64) | **必填**，用户中心主键，索引 |
| `user_name` | string(128) | **冗余快照**，展示/审计用；不保证实时同步 |

- 不存密码、手机号、角色权限明细（权限由用户中心/网关角色声明，如 `X-Role: student|admin|reviewer`）。  
- 管理员操作同样写 `user_id` + `user_name`（操作人）。

### 1.3 ID 与时间

| 约定 | 规则 |
| --- | --- |
| 业务主键 | `ULID` 或 `UUID` 字符串 |
| KG 节点 id | 沿用现有 `{region}:{type}:{source_system}:{source_id}` |
| 时间 | ISO-8601 UTC，字段 `*_at` |
| 软删 | `deleted_at` 可空；查询默认过滤 |

### 1.4 通用响应包

```json
{
  "code": 0,
  "message": "ok",
  "data": {},
  "request_id": "01H..."
}
```

| code | 含义 |
| ---: | --- |
| 0 | 成功 |
| 40001 | 参数错误 |
| 40100 | 未登录/Token 无效 |
| 40300 | 无权限 |
| 40400 | 资源不存在 |
| 40900 | 状态冲突（如重复提交） |
| 42200 | 业务校验失败 |
| 50000 | 服务内部错误 |

分页：

```json
{
  "items": [],
  "page": 1,
  "page_size": 20,
  "total": 100
}
```

---

## 2. 领域知识图（KG）数据结构

> 与 `schemas/graph_schema.yaml` 对齐；本期主路径：**行业 · 专业 · 岗位 · 技能**。

### 2.1 节点 `kg_node`（逻辑视图；物理可继续通用 nodes 表）

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| id | string | Y | 全局唯一 |
| region | enum | Y | `CN` / `EU` / `US` |
| type | enum | Y | `industry` \| `major` \| `occupation` \| `skill_level` \| `course` \| `credential` |
| name | string | Y | 主显示名 |
| name_zh / name_en | string | N | |
| description | text | N | |
| attrs | json | N | 类型扩展（见下） |
| source_system | string | Y | 如 `MOE_CN` `MOHRSS_CN` `BOSS_ZHIPIN` |
| source_id | string | Y | 源侧主键 |
| source_url | string | Y | 可点溯源 |
| license | string | Y | |
| fetched_at | datetime | Y | |
| confidence | enum | Y | `official` \| `derived` \| `ai_inferred` \| `manual_seed` |
| status | enum | Y | `draft` \| `published` \| `archived`（治理用；当前管道默认可视为 published） |
| version | int | Y | 发布版本号，默认 1 |

**attrs 约定（核心类型）**

| type | attrs 关键字段 |
| --- | --- |
| industry | `code`, `level`, `parent_code`, `platform` |
| major | `code`, `level`, `level_zh`, `education_type`, `category`, `catalog` |
| occupation | `code`, `recommend_tier`, `is_green`, `is_digital`, 大典路径 |
| skill_level | `skill_name`, `scale`, `level_code`, `level_zh`, `occupation_code`, `weight_pct` |
| course | `role`(`curriculum_catalog`\|`learnable_resource`), `playable`, `major_code`, … |
| credential | `batch`, `certificate_type`, … |

### 2.2 边 `kg_edge`

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| id | string | Y | |
| src_id / dst_id | string | Y | → kg_node.id |
| rel_type | enum | Y | 见下 |
| region | string | Y | |
| weight | float | N | 0–1 |
| evidence | text | N | 证据摘要 |
| attrs | json | N | 如 `review_status`, `match_method` |
| source_system / source_id / source_url / license / fetched_at / confidence | | Y | 同节点 provenance |
| status | enum | Y | `draft` \| `published` \| `rejected` |
| version | int | Y | |

**rel_type（本期主用）**

| rel_type | 方向 | 含义 |
| --- | --- | --- |
| parent_of | industry → industry | 行业层级 |
| belongs_to | occupation/major → industry | 归属行业 |
| prepares_for | major → occupation | 培养对口 |
| requires | occupation → skill_level | 岗位要求技能 |
| covers | major → skill_level | 专业覆盖技能（可选） |
| related_to | * → * | 弱相关 |
| taught_by | skill_level → course | 后置 |
| leads_to / recognized_by / articulates_to | 课/岗/专 → 证 | 后置 |

### 2.3 KG 逻辑 ER

```
industry ──parent_of──▶ industry
occupation ──belongs_to──▶ industry
major ──belongs_to──▶ industry          （待补）
major ──prepares_for──▶ occupation
occupation ──requires──▶ skill_level
major ──covers──▶ skill_level           （可选）
skill_level ──taught_by──▶ course       （后置）
```

---

## 3. 业务库数据结构

### 3.1 学员画像与诊断

#### `biz_user_snapshot`（可选，首次业务触达时写入）

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| user_id | pk | 用户中心 id |
| user_name | string | 冗余 |
| major_node_id | string N | 默认专业 KG id |
| region | string | 默认 `CN` |
| extra | json | 年级/学校等前端传入 |
| created_at / updated_at | datetime | |

> 不维护登录态；仅缓存展示用冗余。

#### `biz_resume`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | pk | |
| user_id / user_name | | 冗余 |
| file_url | string | 对象存储地址 |
| file_name | string | |
| mime | string | |
| parse_status | enum | `pending` \| `running` \| `success` \| `failed` |
| parse_json | json | 结构化抽取结果（技能、项目、教育…） |
| error_message | text N | |
| created_at / updated_at | | |

#### `biz_chat_session`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | pk | |
| user_id / user_name | | |
| channel | enum | 固定 `chat`；与诊断关联 |
| title | string N | |
| status | enum | `active` \| `closed` |
| created_at / updated_at | | |

#### `biz_chat_message`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | pk | |
| session_id | fk | |
| role | enum | `user` \| `assistant` \| `system` \| `tool` |
| content | text | |
| tool_calls | json N | |
| created_at | | |

#### `biz_diagnosis_session`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | pk | |
| user_id / user_name | | |
| channel | enum | `resume` \| `chat` \| `hybrid` |
| resume_id | fk N | channel=resume 时 |
| chat_session_id | fk N | channel=chat 时 |
| major_node_id | string N | 输入专业 |
| target_occupation_id | string N | 目标岗（可空，由 AI 推荐） |
| status | enum | `created` \| `running` \| `succeeded` \| `failed` |
| error_message | text N | |
| created_at / finished_at | | |

#### `biz_diagnosis_result`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | pk | |
| session_id | fk unique | |
| summary | text | 报告摘要 |
| matched_occupations | json | `[{node_id,name,score,reason}]` |
| skill_gaps | json | 见下 Schema |
| profile_skills | json | 用户侧技能画像快照 |
| raw_model | json N | 模型原始输出（审计/回放） |
| confidence | float N | 0–1 |
| created_at | | |

**skill_gaps 元素 Schema**

```json
{
  "skill_level_id": "CN:skill_level:...",
  "skill_name": "程序编写与修改",
  "required_level_code": "L3",
  "user_level_code": "L1",
  "gap": 2,
  "priority": 1,
  "evidence": "简历未体现 / 对话自评入门"
}
```

#### `biz_user_skill_profile`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | pk | |
| user_id / user_name | | |
| skill_level_id | string N | 优先挂 KG |
| skill_name | string | 冗余名，防 KG 变更 |
| level_code | string | L1–L4 或国标 L5… |
| source | enum | `resume` \| `chat` \| `self` \| `path` |
| score | float N | |
| diagnosis_id | fk N | 最近一次更新来源 |
| updated_at | | |

唯一建议：`(user_id, skill_name, level_code)` 或 `(user_id, skill_level_id)`。

---

### 3.2 学习路径与进度

#### `biz_learning_path`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | pk | |
| user_id / user_name | | |
| diagnosis_id | fk N | |
| target_occupation_id | string | KG occupation |
| title | string | |
| status | enum | `draft` \| `active` \| `paused` \| `completed` \| `abandoned` |
| progress_pct | float | 0–100，可冗余计算 |
| created_at / updated_at | | |

#### `biz_path_step`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | pk | |
| path_id | fk | |
| seq | int | 从 1 递增 |
| step_type | enum | `course` \| `practice` \| `project` \| `credential` \| `skill` \| `other` |
| ref_node_id | string N | KG 节点 id |
| title | string | |
| description | text N | |
| est_hours | float N | |
| status | enum | `todo` \| `doing` \| `done` \| `skipped` |
| completed_at | datetime N | |

#### `biz_progress_event`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | pk | |
| user_id / user_name | | |
| event_type | enum | `step_start` \| `step_complete` \| `skill_level_up` \| `path_complete` \| … |
| path_id / step_id | fk N | |
| skill_level_id | string N | |
| payload | json | |
| created_at | | |

---

### 3.3 成就与游戏化

#### `biz_achievement_def`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | pk | |
| code | string unique | 如 `first_diagnosis` |
| name | string | |
| description | text | |
| icon_url | string N | |
| points | int | 奖励积分 |
| rule_json | json | 触发规则（事件类型+条件） |
| enabled | bool | |
| created_at | | |

#### `biz_user_achievement`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | pk | |
| user_id / user_name | | |
| achievement_id | fk | |
| unlocked_at | datetime | |
| meta | json N | |

唯一：`(user_id, achievement_id)`。

#### `biz_points_ledger`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | pk | |
| user_id / user_name | | |
| delta | int | 可负 |
| balance_after | int | |
| reason | string | |
| ref_type / ref_id | string N | 关联成就/步骤 |
| created_at | | |

#### `biz_user_streak`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| user_id | pk | |
| user_name | string | |
| current_streak | int | |
| longest_streak | int | |
| last_active_date | date | 用户时区或 UTC 日 |

---

### 3.4 后台：AI 建模提案与审核发布

#### `biz_model_proposal`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | pk | |
| proposal_type | enum | `node_create` \| `node_update` \| `edge_create` \| `edge_update` \| `edge_delete` |
| payload | json | 完整节点/边草稿（同 KG 字段） |
| confidence | enum | |
| evidence | text N | |
| source | enum | `ai` \| `rule` \| `human` \| `import` |
| status | enum | `pending` \| `approved` \| `rejected` \| `cancelled` |
| created_by_id / created_by_name | | 操作人冗余 |
| created_at | | |

#### `biz_review_decision`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | pk | |
| proposal_id | fk | |
| decision | enum | `approve` \| `reject` |
| comment | text N | |
| reviewer_id / reviewer_name | | |
| decided_at | | |

#### `biz_publish_bundle`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | pk | |
| version_tag | string | 如 `2026.08.06.1` |
| summary | text | |
| node_count / edge_count | int | |
| manifest | json | 本批发布的 node_id/edge_id 列表或 diff |
| publisher_id / publisher_name | | |
| published_at | | |
| status | enum | `published` \| `rolled_back` |

**发布语义：** `approve` 后将 payload 写入 KG（`status=published`），并记入 bundle；拒绝则只改 proposal 状态。

#### `biz_validation_rule` / `biz_validation_run`

| 表 | 要点 |
| --- | --- |
| rule | `code`, `name`, `severity`, `expr_or_script`, `enabled` |
| run | `rule_id`, `started_at`, `finished_at`, `status`, `result_json`（违规列表） |

示例规则：孤立岗位占比、AI 边未审核超阈值、环边、同 id 冲突。

---

### 3.5 运营埋点

#### `biz_analytics_event`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | pk | |
| user_id / user_name | N 可空（匿名浏览） | |
| event | string | `explore_search` `diagnosis_start` `path_step_done` … |
| props | json | |
| created_at | | |

---

### 3.6 业务库 ER 关系（简图）

```
user_id(外部)
    │
    ├─ biz_resume ──▶ biz_diagnosis_session ──▶ biz_diagnosis_result
    ├─ biz_chat_session ──▶ biz_chat_message
    │         └──────────────┘
    ├─ biz_user_skill_profile
    ├─ biz_learning_path ──▶ biz_path_step
    ├─ biz_progress_event
    ├─ biz_user_achievement ──▶ biz_achievement_def
    └─ biz_points_ledger

admin(user_id)
    ├─ biz_model_proposal ──▶ biz_review_decision
    └─ biz_publish_bundle
```

---

## 4. 接口设计

### 4.0 公共

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| GET | `/health` | 存活 + KG 连通 |
| GET | `/v1/meta/enums` | 返回 type/rel/confidence/status 等枚举 |

**鉴权 Header（建议）**

```
Authorization: Bearer <user_center_token>
# 或网关注入：
X-User-Id: ...
X-User-Name: ...
X-Roles: student,admin
```

服务端写库时：`user_id` 以 Token/网关为准；`user_name` 取网关或请求体快照。

---

### 4.0 增量接口（2026-08 落地）

#### 图探索（常规形态，对齐 Graph Explorer）

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| GET | `/v1/search?q=` | **Search**：只返回匹配节点，不扩边；可 `type` |
| POST | `/v1/graph/expand` | **Expand**：body `ExpandRequest`，**1 跳**邻居 + 边，`limit` 默认 25 |
| GET | `/v1/graph/explore` | **默认 depth=0** 仅种子；`depth≥1` 为兼容旧 BFS 子图（管理端已不用作默认） |

参考：[aws/graph-explorer](https://github.com/aws/graph-explorer)、neo4j-labs create-context-graph `POST /expand`。

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| GET | `/v1/student/industries` | 行业分页 + `counts.major/occupation` |
| GET | `/v1/student/professions` | 专业 + `counts.occupation/skill` |
| GET | `/v1/student/positions` | 岗位 + `industries[]` + `counts.skill` |
| GET | `/v1/student/positions/{id}/skill-composition` | 技能构成；`weight` 只认 requires 边；`weight_sum` |
| GET | `/v1/student/skills?view=bundle` | 逻辑技能聚合（默认） |
| GET | `/v1/student/skills/bundles/{key}` | 含 levels / prerequisites |
| POST | `/v1/student/diagnosis/resume` | 简历诊断；AI 网关 `create_react_agent` 或规则 |
| POST | `/v1/admin/skills` | 多档创建进审；`levels.L*{}` + `occupation_links.weight` |
| PATCH | `/v1/admin/skills/{key}` | 多档更新进审 |
| GET/POST/PUT/DELETE | `/v1/admin/skills/{key}/prerequisites` | 先修无环 |
| GET | `/v1/admin/edges/review` | AI/低置信边抽检 |
| GET | `/v1/admin/ai-gateway` | AI 网关就绪（非 UC） |
| GET | `/v1/kg/nodes?include_counts=1` | 管理列表联读 counts |

**AI 网关 env**：`LLM_BASE_URL`、`GITHUB_TOKEN`、`LLM_MODEL`（对齐 bcs-ai-agent）。见 `docs/AI网关与诊断Agent.md`。

**权重**：岗位技能构成占比 → `occupation -requires→ skill` 边 `weight`；节点 `attrs.weight_pct` 仅历史兼容。

### 4.1 学员端 · 探索（KG 只读）

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| GET | `/v1/search` | 关键字搜索节点 |
| GET | `/v1/graph/explore` | 搜索 + 邻域展开 |
| GET | `/v1/graph/by-major` | 以专业为根的子图 |
| GET | `/v1/nodes/{id}` | 节点详情 |
| GET | `/v1/occupations/{id}/skills` | 岗位要求技能列表（requires） |
| GET | `/v1/majors/{id}/occupations` | 专业对口岗位（prepares_for） |
| GET | `/v1/industries` | 行业树 |
| GET | `/v1/industries/{id}/occupations` | 行业下岗位 |
| GET | `/v1/stats` | 图规模统计 |

#### `GET /v1/search`

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| q | string | 必填 |
| region | string | 默认 CN |
| type | string | industry/major/occupation/skill_level/… |
| limit | int | 默认 20 |

**响应 `data`：** `KgNode[]`（字段同 2.1，可裁剪 attrs）

#### `GET /v1/graph/explore`

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| q | string | 关键字；空表示 list |
| type | string | 种子类型 |
| region | string | |
| depth | int | 0–4 |
| max_nodes | int | |

**响应 `data`：**

```json
{
  "roots": [],
  "nodes": [],
  "edges": [],
  "paths": [],
  "meta": { "matched": 0, "truncated": false }
}
```

#### `GET /v1/occupations/{id}/skills`

**响应：**

```json
{
  "occupation": { "id": "", "name": "" },
  "skills": [
    {
      "skill_level_id": "",
      "skill_name": "",
      "level_code": "L3",
      "weight": 0.8,
      "evidence": ""
    }
  ]
}
```

---

### 4.2 学员端 · 诊断（双通道）

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| POST | `/v1/diagnosis/resume` | 上传简历并发起诊断 |
| GET | `/v1/resumes/{id}` | 简历与解析状态 |
| POST | `/v1/diagnosis/chat/sessions` | 创建对话诊断会话 |
| POST | `/v1/diagnosis/chat/sessions/{id}/messages` | 发消息（可流式） |
| GET | `/v1/diagnosis/chat/sessions/{id}/messages` | 历史消息 |
| POST | `/v1/diagnosis/sessions/{id}/run` | 触发/重跑出报告 |
| GET | `/v1/diagnosis/sessions/{id}` | 会话状态 |
| GET | `/v1/diagnosis/sessions/{id}/result` | 诊断结果 |
| GET | `/v1/diagnosis/sessions` | 我的诊断列表 |
| GET | `/v1/me/skills` | 当前技能画像 |
| PUT | `/v1/me/skills` | 自评批量更新 |

#### `POST /v1/diagnosis/resume`

- Content-Type: `multipart/form-data`  
- 字段：`file`, `major_node_id?`, `target_occupation_id?`, `user_name?`（可忽略，以网关为准）

**响应：**

```json
{
  "resume_id": "",
  "session_id": "",
  "status": "running"
}
```

异步：解析完成后写 `biz_diagnosis_result`，可 Webhook/轮询 `GET .../result`。

#### `POST /v1/diagnosis/chat/sessions/{id}/messages`

**请求：**

```json
{
  "content": "我想做Java后端，做过课程设计"
}
```

**响应（非流式）：**

```json
{
  "message_id": "",
  "reply": { "role": "assistant", "content": "..." },
  "diagnosis_ready": false
}
```

流式：`Accept: text/event-stream`，SSE 推送 token；结束后可带 `diagnosis_session_id`。

#### `GET /v1/diagnosis/sessions/{id}/result`

**响应 `data`：** 同 `biz_diagnosis_result` 公开字段 + 展开岗位/技能名称。

---

### 4.3 学员端 · 学习路径与进度

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| POST | `/v1/paths/generate` | 根据诊断结果生成路径 |
| GET | `/v1/paths` | 我的路径列表 |
| GET | `/v1/paths/{id}` | 路径详情（含 steps） |
| PATCH | `/v1/paths/{id}` | 暂停/恢复/放弃 |
| POST | `/v1/paths/{id}/steps/{step_id}/start` | 开始步骤 |
| POST | `/v1/paths/{id}/steps/{step_id}/complete` | 完成步骤 |
| GET | `/v1/me/progress` | 汇总进度 |

#### `POST /v1/paths/generate`

**请求：**

```json
{
  "diagnosis_id": "",
  "target_occupation_id": "",
  "max_steps": 12
}
```

**逻辑：** 读 `skill_gaps` → 按 priority 排序 → 匹配 KG 资源（course/credential）生成 `biz_path_step`。

#### `POST .../complete`

**请求：**

```json
{ "note": "optional" }
```

副作用：写 `progress_event`、更新 `path.progress_pct`、可能触发成就与积分。

---

### 4.4 学员端 · 成就游戏化

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| GET | `/v1/achievements` | 成就定义列表（含是否已解锁） |
| GET | `/v1/me/points` | 积分余额 + 近 20 条流水 |
| GET | `/v1/me/streak` | 连续活跃 |

---

### 4.5 后台 · 图谱只读增强（运维/教研）

在 4.1 基础上：

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| GET | `/v1/admin/kg/coverage` | 覆盖率指标（专→岗、岗→技、行业…） |
| GET | `/v1/admin/kg/nodes` | 分页筛选节点 |
| GET | `/v1/admin/kg/edges` | 分页筛选边（含 confidence/status） |

**覆盖率 `data` 示例：**

```json
{
  "region": "CN",
  "nodes": { "industry": 149, "major": 2281, "occupation": 1639, "skill_level": 8919 },
  "edges": { "prepares_for": 6939, "requires": 9938, "belongs_to": 1159 },
  "coverage": {
    "major_with_prepares_for": 0.596,
    "occupation_with_requires": 0.298
  }
}
```

---

### 4.6 后台 · 模型维护与 AI 提案

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| POST | `/v1/admin/kg/nodes` | 人工创建节点（进 draft 或 proposal） |
| PATCH | `/v1/admin/kg/nodes/{id}` | 更新草稿 |
| POST | `/v1/admin/kg/edges` | 人工创建边 |
| POST | `/v1/admin/modeling/jobs` | 触发 AI/规则建模任务 |
| GET | `/v1/admin/modeling/jobs/{id}` | 任务状态 |
| GET | `/v1/admin/proposals` | 提案列表 `?status=pending` |
| GET | `/v1/admin/proposals/{id}` | 提案详情 |
| POST | `/v1/admin/proposals/{id}/approve` | 审核通过 → 写 KG |
| POST | `/v1/admin/proposals/{id}/reject` | 驳回 |
| POST | `/v1/admin/publish` | 打版本发布包 |
| GET | `/v1/admin/publish` | 发布历史 |
| POST | `/v1/admin/validate` | 跑校验规则 |
| GET | `/v1/admin/validate/runs` | 历史运行 |

#### `POST /v1/admin/modeling/jobs`

**请求：**

```json
{
  "job_type": "link_prepares_for_llm",
  "params": { "limit": 200, "region": "CN" }
}
```

**响应：** `{ "job_id": "", "status": "queued" }`  
完成后批量写入 `biz_model_proposal`（status=pending），**不直接污染已发布边**（推荐模式）。

#### `POST /v1/admin/proposals/{id}/approve`

**请求：**

```json
{ "comment": "抽检通过" }
```

事务：写 `biz_review_decision` + upsert KG + proposal.status=approved。

---

### 4.7 后台 · 权限说明（不建用户表）

| 接口前缀 | 要求角色（示例） |
| --- | --- |
| `/v1/diagnosis/**` `/v1/paths/**` `/v1/me/**` `/v1/achievements` | `student` 或已登录用户 |
| `/v1/admin/**` | `admin` 或 `reviewer` |
| `approve/reject/publish` | `admin` 或 `reviewer` |
| `modeling/jobs` `kg` 写 | `admin` |

角色来自用户中心 Token claims，本服务只做鉴权注解。

---

### 4.8 运营

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| POST | `/v1/events` | 前端埋点上报 |
| GET | `/v1/admin/dashboard/summary` | 看板：DAU、诊断数、路径完成率、覆盖率摘要 |

#### `POST /v1/events`

```json
{
  "event": "diagnosis_start",
  "props": { "channel": "resume" }
}
```

`user_id/user_name` 由网关填充。

---

## 5. 与现有实现的映射

| 现有 | 新设计位置 |
| --- | --- |
| `GET /v1/search` `/v1/graph/explore` `/v1/stats` | 保留，归入 4.1；扩展 industry |
| SQLite nodes/edges | KG 物理表，增加 `status/version` 字段（迁移） |
| `link_*` 脚本 | 演进为 `POST /v1/admin/modeling/jobs` 的 worker |
| `reports/*覆盖率*` | `GET /v1/admin/kg/coverage` |
| 无用户表 | **保持**；业务表仅 `user_id`+`user_name` |

---

## 6. 接口优先级（服务端落地）

| 批次 | 接口组 | 目的 |
| --- | --- | --- |
| **M1** | 4.1 探索增强 + `/nodes/{id}` + occupation skills + industries | 探索可用 |
| **M2** | 4.2 诊断（可先同步 mock 结果）+ `/me/skills` | 双通道诊断闭环 |
| **M3** | 4.3 路径生成与 complete | 学习路径 |
| **M4** | 4.6 提案审核发布 | 后台治理 |
| **M5** | 4.4 成就 + 4.8 看板 | 游戏化与运营 |

---

## 7. OpenAPI 目录树（速查）

```
/health
/v1/meta/enums
/v1/search
/v1/graph/explore
/v1/graph/by-major
/v1/nodes/{id}
/v1/majors/{id}/occupations
/v1/occupations/{id}/skills
/v1/industries
/v1/industries/{id}/occupations
/v1/stats

/v1/diagnosis/resume
/v1/diagnosis/chat/sessions
/v1/diagnosis/chat/sessions/{id}/messages
/v1/diagnosis/sessions
/v1/diagnosis/sessions/{id}
/v1/diagnosis/sessions/{id}/result
/v1/diagnosis/sessions/{id}/run
/v1/resumes/{id}
/v1/me/skills

/v1/paths/generate
/v1/paths
/v1/paths/{id}
/v1/paths/{id}/steps/{step_id}/start
/v1/paths/{id}/steps/{step_id}/complete
/v1/me/progress

/v1/achievements
/v1/me/points
/v1/me/streak

/v1/events

/v1/admin/kg/coverage
/v1/admin/kg/nodes
/v1/admin/kg/edges
/v1/admin/kg/nodes|edges  (POST/PATCH)
/v1/admin/modeling/jobs
/v1/admin/proposals
/v1/admin/proposals/{id}/approve|reject
/v1/admin/publish
/v1/admin/validate
/v1/admin/dashboard/summary
```

---

## 8. 附录 · 业务库建表清单

```
biz_user_snapshot
biz_resume
biz_chat_session
biz_chat_message
biz_diagnosis_session
biz_diagnosis_result
biz_user_skill_profile
biz_learning_path
biz_path_step
biz_progress_event
biz_achievement_def
biz_user_achievement
biz_points_ledger
biz_user_streak
biz_model_proposal
biz_review_decision
biz_publish_bundle
biz_validation_rule
biz_validation_run
biz_analytics_event
```

KG 侧继续：`nodes` / `edges`（建议加 `status`, `version`）。

---

*实现时建议先出 OpenAPI 3.0 YAML 与 Flyway/Liquibase DDL；本文件为权威设计说明。*
