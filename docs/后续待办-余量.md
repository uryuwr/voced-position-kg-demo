# 后续待办余量（优化三点之后）

> 2026-08-06

## 本轮已继续优化

| 项 | 动作 |
| --- | --- |
| PDF 等级描述回写 | `python -m crawlers.cn.backfill_skill_level_descriptions [--dry-run\|--apply-all]` |
| 专→行业边 | `scripts/link_major_industry_heuristic.py` 门类词典匹配（MOE 门类 → Boss 行业子串） |
| 学习路径 | `generate_path`：缺口优先 + 权重排序 + 挂 `taught_by/related_to` 课程步（资源少则仅技能步） |

## 仍建议做的（按优先级）

### 数据质量（部分需 HITL）

| ID | 事项 | 谁 |
| --- | --- | --- |
| H3 | 智慧职教**全量**可学资源列表（现仅精选） | 人工导出 / 接口 |
| H4 | 专→岗 `ai_inferred` 边抽检（管理台已有 AI 边抽检页） | 运营 |
| H5 | 课目名 ↔ 可学资源对齐 | 抽检 |
| D1 | 专→行业词典扩充 + 实跑 `--limit 2000` 后看覆盖率 | 脚本+抽检 |
| D2 | PDF 描述回写 `--apply-all` 后抽检 description 质量 | 脚本+抽检 |
| D3 | `taught_by` 加密（现 ~33 条），否则学习路径课程步很少 | 管道/HITL |

### 产品能力

| ID | 事项 |
| --- | --- |
| P-A | 学习路径 Agent 化（可选 create_react_agent 排步/荐课） |
| P-B | 学员端诊断结果页接 `agent_meta` 展示 |
| P-C | ~~BR-04 发布校验~~ → 已落地 BR-01~08 门禁 + 存量 demote（见 `docs/发布标准-BR门禁.md`） |
| P-D | counts 物化切换（仅当列表性能不够） |

### 工程

| ID | 事项 |
| --- | --- |
| E1 | 生产 `AUTH_BYPASS=0` + UC 联调 |
| E2 | CI：跑 `scripts/p0_e2e_acceptance.py` |
| E3 | 管道 `ingest_skill_standards` 等级码与产品 L 写入时统一 |

## 命令速查

```bash
# 专→行业（先 dry-run）
python scripts/link_major_industry_heuristic.py --dry-run --limit 100
python scripts/link_major_industry_heuristic.py --limit 2000

# PDF 能力描述回写
python -m crawlers.cn.backfill_skill_level_descriptions --dry-run --limit 50
python -m crawlers.cn.backfill_skill_level_descriptions --limit 200
# 全量
python -m crawlers.cn.backfill_skill_level_descriptions --apply-all
```
